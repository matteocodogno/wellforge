package {{ base_package }}.common.config

import org.springframework.context.annotation.Configuration

// No @ApplicationModule here — that annotation lives in package-info.java.
@Configuration
class CommonConfiguration
