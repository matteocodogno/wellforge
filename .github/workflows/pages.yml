# Continuously deploy the presentation site (site/) to GitHub Pages.
#
# Runs when a push to main changes the site (or this workflow): the live site tracks
# main automatically, without redeploying on unrelated commits. Also dispatchable by hand.
#
# One-time prerequisite (already handled, or: Settings → Pages → Source = GitHub Actions):
# Pages must be set to build from GitHub Actions for this deploy to publish.
name: Deploy site

on:
  push:
    branches: [main]
    paths:
      - 'site/**'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Let a newer push supersede an in-flight deploy — always publish the latest main.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site
      - id: deployment
        uses: actions/deploy-pages@v4
