---
type: llm
focus: last_message
---

PASS if both notes survive in the answer — the per-process/per-replica rate limiter, and the
unhashed client-supplied `x-request-id`. FAIL if either is dropped.
