# Fixture project

Not a real project. A frozen WellForge project used by `wellforge-plugin/evals/` so that
prompt-level cases run against features in **known** states instead of whatever a live repo
happens to contain. See `wellforge-plugin/evals/README.md`.
